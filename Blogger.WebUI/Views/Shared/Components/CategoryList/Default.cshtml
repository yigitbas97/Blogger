@model Blogger.WebUI.Models.CategoryListViewModel;

@{
    foreach (var category in Model.Categories)
    {
        var css = "list-group-item";
        <li>
            <a href="/post/index?categoryId=@category.Id&page=1" class="@css">
                @category.Name
            </a>
        </li>
    }
}
